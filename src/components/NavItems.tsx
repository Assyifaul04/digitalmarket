"use client"

import { useState } from "react";

const NavItems = () => {
  const [activeIndex, setAtiveIndex] = useState<null | number>(null);
  return <div className="flex gap-4 h-full"></div>;
}

export default NavItems;
